# Core API — cloud deployment (Render)
# Voice packages (whisper, pyaudio, sounddevice, openwakeword) are NOT included
# because the cloud server has no microphone or speaker. Voice runs locally on your PC/phone.

groq>=0.9.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pytz>=2024.1
requests>=2.31.0